services:
  - type: web
    name: mavi-lp
    runtime: static
    buildCommand: echo "Static site — no build needed"
    staticPublishPath: .
    headers:
      - path: /assets/*
        name: Cache-Control
        value: public, max-age=2592000
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
